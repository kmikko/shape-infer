{
  "name": "schema-generator",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.6.2",
  "description": "Infer a unified TypeScript schema from JSONL records.",
  "bin": {
    "schema-gen": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "smoke": "pnpm run build && node dist/cli.js --input fixtures/sample.jsonl --type-name SampleRecord"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
